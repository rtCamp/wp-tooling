<?xml version="1.0"?>
<ruleset name="Project">
	<description>Extends rtCamp shared standard: WordPress-Core, Extra, Docs, and VIP-Go rules.</description>

	<!--
		Scaffolded by @rtcamp/wp-tooling. The shared rules live at
		vendor/rtcamp/wp-php-toolkit/phpcs.xml. Override a rule for this
		project by adding it below the include line.
	-->

	<rule ref="vendor/rtcamp/wp-php-toolkit/phpcs.xml"/>

	<file>./</file>
	<exclude-pattern>*/vendor/*</exclude-pattern>
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/build/*</exclude-pattern>
	<exclude-pattern>*/tests/*</exclude-pattern>

	<!-- Project-specific overrides go here. -->
</ruleset>
