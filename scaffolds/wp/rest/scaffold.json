{
	"slug": "rest",
	"category": "wp",
	"name": "REST controller",
	"description": "Adds a REST API controller extending Abstract_REST_Controller from rtcamp/wp-php-toolkit. Singleton by default (REST controllers wire hooks once per request).",
	"source": "template",
	"inputs": [
		{
			"key": "namespace",
			"description": "PSR-4 namespace prefix.",
			"discover_from": "composer.json:autoload.psr-4",
			"default": "Inc"
		},
		{
			"key": "base_path",
			"description": "PHP source base directory.",
			"discover_from": "composer.json:autoload.psr-4",
			"default": "includes"
		},
		{
			"key": "name",
			"description": "Resource slug (kebab-case, used as the REST route segment, e.g. 'images' for /wp-json/myplugin/v1/images).",
			"required": true
		},
		{
			"key": "class",
			"description": "PascalCase class name, derived from name.",
			"discover_from": "input:name",
			"transform": "pascal-case"
		},
		{
			"key": "route_namespace",
			"description": "REST API namespace (e.g. 'myplugin/v1'). Used as the first argument to register_rest_route().",
			"required": true
		},
		{
			"key": "singleton",
			"description": "Use rtcamp/wp-php-toolkit Singleton trait. Pass 'true' or 'false'. Default 'true' is appropriate for most REST controllers (one controller per resource, hook wiring happens once).",
			"default": "true"
		}
	],
	"files": [
		{
			"src": "templates/controller.php.mustache",
			"dest": "{{base_path}}/Rest/{{class}}Controller.php"
		}
	],
	"wiring": [
		{
			"target_file": "{{base_path}}/Plugin.php",
			"anchor": "// scaffold:rest-controllers",
			"snippet_template": "{{#singleton}}\\{{namespace}}\\Rest\\{{class}}Controller::get_instance();{{/singleton}}{{^singleton}}( new \\{{namespace}}\\Rest\\{{class}}Controller() )->register();{{/singleton}}",
			"description": "Instantiate the REST controller so it registers its rest_api_init hook. Insert in the plugin bootstrap method alongside other REST controllers. Singleton variants self-register on first get_instance(); multi-instance callers explicitly invoke register()."
		}
	],
	"tests": [
		{
			"src": "templates/test.php.mustache",
			"dest": "tests/Rest/{{class}}ControllerTest.php",
			"framework": "phpunit"
		}
	],
	"composer_dependencies": {
		"rtcamp/wp-php-toolkit": "^1.0"
	}
}
