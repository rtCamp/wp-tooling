{
    "name": "wp-tooling-monorepo",
    "version": "0.0.0",
    "description": "rtCamp WordPress tooling monorepo — Node packages (npm workspaces) + Composer packages (monorepo-builder + splitsh-lite).",
    "license": "GPL-2.0-or-later",
    "author": "rtCamp",
    "private": true,
    "engines": {
        "node": ">=22"
    },
    "workspaces": [
        "node-packages/*"
    ],
    "scripts": {
        "lint": "npm run lint --workspaces --if-present",
        "lint:fix": "npm run lint:fix --workspaces --if-present",
        "test": "npm test --workspaces --if-present",
        "test:coverage": "npm run test:coverage --workspaces --if-present",
        "check": "npm run check --workspaces --if-present"
    },
    "// overrides": "Forces minimatch off the vulnerable 9.0.0-9.0.6 range (3 high-severity ReDoS CVEs reaching us via the @typescript-eslint/typescript-estree chain that @wordpress/eslint-plugin v22 pulls in). Lives at the workspace root because npm only honours `overrides` in the root package.json. Remove once @wordpress/eslint-plugin can move to v25 (gated on @wordpress/scripts supporting ESLint 9).",
    "overrides": {
        "minimatch": "^9.0.7"
    }
}
