#!/usr/bin/env sh
# Runs `npm run lint:staged` when the script is defined in package.json.
# No-op (exit 0) otherwise, keeping pure-PHP repos working out of the box.
if [ -f package.json ]; then
    npm run lint:staged --if-present --silent
fi
