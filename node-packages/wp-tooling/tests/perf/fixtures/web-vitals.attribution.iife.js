/**
 * Fixture stand-in for the web-vitals attribution IIFE build — content is
 * never executed in these tests (collect-vitals.js is mocked), it only
 * needs to be a real, readable file for fs.readFileSync.
 */
globalThis.webVitals = {};
