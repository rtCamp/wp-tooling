/**
 * Stylelint config: extends rtCamp's shared @rtcamp/stylelint-config.
 *
 * Scaffolded by @rtcamp/wp-tooling. The shared rules live in
 * node_modules/@rtcamp/stylelint-config. To override a rule for this
 * project, add it under `rules` below.
 */
'use strict';

module.exports = {
	extends: ['@rtcamp/stylelint-config'],
	rules: {
		// Project-specific overrides go here.
	},
};
