<?xml version="1.0"?>
<ruleset name="Project">
	<description>WordPress combined standard: Core, Extra, and Docs rules from wp-coding-standards/wpcs.</description>

	<!--
		Scaffolded by @rtcamp/wp-tooling. Extend or override rules below.
		Switch to lint/phpcs/full if your project needs VIP-Go rules.
	-->

	<rule ref="WordPress"/>

	<file>./</file>
	<exclude-pattern>*/vendor/*</exclude-pattern>
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/build/*</exclude-pattern>
	<exclude-pattern>*/tests/*</exclude-pattern>

	<!-- Project-specific overrides go here. -->
</ruleset>
