/**
 * Script module "@{{text_domain}}/{{module_slug}}".
 *
 * A script module is a native ES module: WordPress loads it with
 * `type="module"`, so it is deferred and its top-level code runs once.
 *
 * Bare specifiers for modules WordPress registers -- '@wordpress/interactivity'
 * and friends -- resolve through the import map core prints, so importing them
 * needs no bundler. Any such import must also be declared as a dependency in
 * the PHP registration (or carried there by the build's .asset.php).
 *
 * Scaffolded by @rtcamp/wp-tooling.
 *
 * @see https://developer.wordpress.org/reference/functions/wp_register_script_module/
 */

/**
 * Replace with this module's work.
 */
export function init() {
	// Intentionally empty: the scaffold ships the wiring, not the behaviour.
}

init();
